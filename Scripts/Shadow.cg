!lib ChickenGun;

pub class Module_Shadow
{
  pub int _SChance;
  pub PlayerHandler CurrentRoom;
  pub void CloneToRoom()
  {
    ScriptParent:Clone(CurrentScene._GenRooms:CurrentRoom.EntiteShadowSP + 1):WithChance = _SChance;
    OnCloned(ScriptParent).SetTransform(JupscareSP);
  }
}